Flask==3.0.0
Flask-Cors==3.0.10
Flask-SQLAlchemy==3.0.3
openai==1.9.0
requests==2.31.0
python-dotenv
# Pillow and pytesseract are optional for local OCR fallback only.
# They are provided in requirements-optional.txt so Vercel doesn't attempt to build them.
psycopg2-binary==2.9.6
